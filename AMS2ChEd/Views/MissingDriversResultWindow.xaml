<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AMS2ChEd.Views.MissingDriversResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Missing Drivers"
        Height="520" Width="660"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a1a"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SectionHeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#c41e3a"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="RowText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="DriverCombo" TargetType="ComboBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="RowCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ConfirmButton" TargetType="Button">
            <Setter Property="Background" Value="#c41e3a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="30,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#d32f2f"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="MANUAL RESULTS ENTRY"
                       FontSize="22" FontWeight="Bold"
                       Foreground="#c41e3a"
                       HorizontalAlignment="Center"/>
            <Rectangle Height="2" Fill="#c41e3a" Width="200" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="The following positions could not be identified in AMS2. Assign the missing driver to each position. Leave a position blank to skip it."
                   TextWrapping="Wrap"
                   Foreground="#aaaaaa"
                   FontSize="12"
                   Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel x:Name="QualiSection" Margin="0,0,0,20">
                    <TextBlock Text="MISSING QUALIFYING POSITIONS" Style="{StaticResource SectionHeaderText}"/>
                    <ItemsControl x:Name="QualiPanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#333333" BorderThickness="0,0,0,1" Padding="0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Position, StringFormat='P{0}'}"
                                                   Style="{StaticResource RowText}"/>
                                        <ComboBox Grid.Column="1"
                                                  ItemsSource="{Binding AvailableDrivers}"
                                                  DisplayMemberPath="DisplayName"
                                                  SelectedItem="{Binding SelectedDriver}"
                                                  Style="{StaticResource DriverCombo}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <StackPanel x:Name="RaceSection">
                    <TextBlock Text="MISSING RACE POSITIONS" Style="{StaticResource SectionHeaderText}"/>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1" Text="DRIVER" Style="{StaticResource HeaderText}" HorizontalAlignment="Left"/>
                        <TextBlock Grid.Column="2" Text="DNF" Style="{StaticResource HeaderText}"/>
                        <TextBlock Grid.Column="3" Text="FASTEST LAP" Style="{StaticResource HeaderText}"/>
                    </Grid>

                    <ItemsControl x:Name="RacePanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#333333" BorderThickness="0,0,0,1" Padding="0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="80"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Position, StringFormat='P{0}'}"
                                                   Style="{StaticResource RowText}"/>
                                        <ComboBox Grid.Column="1"
                                                  ItemsSource="{Binding AvailableDrivers}"
                                                  DisplayMemberPath="DisplayName"
                                                  SelectedItem="{Binding SelectedDriver}"
                                                  Style="{StaticResource DriverCombo}"/>
                                        <CheckBox Grid.Column="2"
                                                  IsChecked="{Binding IsDnf}"
                                                  Style="{StaticResource RowCheck}"/>
                                        <CheckBox Grid.Column="3"
                                                  IsChecked="{Binding IsFastestLap}"
                                                  Style="{StaticResource RowCheck}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="3"
                Content="CONFIRM RESULTS"
                Style="{StaticResource ConfirmButton}"
                HorizontalAlignment="Center"
                Margin="0,20,0,0"
                Click="ConfirmButton_Click"/>
    </Grid>
</Window>
