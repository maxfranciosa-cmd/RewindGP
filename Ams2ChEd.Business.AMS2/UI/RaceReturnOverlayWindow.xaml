<Window x:Class="Ams2ChEd.Business.AMS2.UI.RaceReturnOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Ams2ChEd.Business.AMS2.Localization"
        Title="Rewind GP"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="OverlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#c41e3a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="30,15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#d32f2f"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="#1a1a1a" BorderBrush="#c41e3a" BorderThickness="3"
                CornerRadius="8" Padding="40" HorizontalAlignment="Center" VerticalAlignment="Center"
                MaxWidth="600">
            <StackPanel>
                <TextBlock Text="{loc:Loc RaceReturnOverlayWindow_SessionFinished}"
                           Foreground="White" FontSize="22" FontWeight="Bold"
                           TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,25"/>
                <Button Content="{loc:Loc RaceReturnOverlayWindow_ReturnButton}"
                        Style="{StaticResource OverlayButtonStyle}" HorizontalAlignment="Center"
                        Click="ReturnButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
