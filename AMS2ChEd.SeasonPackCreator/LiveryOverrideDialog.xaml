<Window x:Class="AMS2ChEd.SeasonPackEditor.LiveryOverrideDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Livery Override Editor" Height="650" Width="600" WindowStartupLocation="CenterOwner" Background="#1A1A1A">
    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#404040"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,3"/>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#c41e3a"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                Background="#2a2a2a"
                                BorderBrush="#c41e3a"
                                BorderThickness="2"
                                Focusable="False"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid>
                                            <Border x:Name="Border" 
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"/>
                                            <Path x:Name="Arrow" 
                                        HorizontalAlignment="Right" 
                                        VerticalAlignment="Center"
                                        Margin="0,0,10,0"
                                        Data="M 0 0 L 4 4 L 8 0 Z"
                                        Fill="White"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                    IsHitTestVisible="False"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    Margin="5,3,23,3"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup x:Name="Popup"
                         Placement="Bottom"
                         IsOpen="{TemplateBinding IsDropDownOpen}"
                         AllowsTransparency="True"
                         Focusable="False"
                         PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                            SnapsToDevicePixels="True"
                            MinWidth="{TemplateBinding ActualWidth}"
                            MaxHeight="200">
                                    <Border x:Name="DropDownBorder"
                                  Background="#2a2a2a"
                                  BorderBrush="#c41e3a"
                                  BorderThickness="2"/>
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="#2a2a2a"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Border" Background="{TemplateBinding Background}" BorderThickness="0">
                                    <ContentPresenter Margin="5,3,5,3"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#c41e3a"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#d32f2f"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Style.Resources>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Race-Specific Livery Override" FontSize="18" FontWeight="Bold" Foreground="#E10600" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Race Selection -->
                <Label Content="Races (select multiple):"/>
                <Border Background="#2D2D2D" BorderBrush="#404040" BorderThickness="1" Padding="10" Margin="0,0,0,15" MaxHeight="150">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Name="RacesCheckBoxPanel"/>
                    </ScrollViewer>
                </Border>

                <!-- Driver 1 Livery -->
                <Label Content="Driver 1 Livery:"/>
                <StackPanel Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Name="Driver1LiveryTextBox" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Browse..." Style="{StaticResource SecondaryButton}" Click="BrowseDriver1Livery_Click"/>
                    </Grid>
                    <TextBlock Name="Driver1LiverySourceLabel" Text="" Foreground="#888888" FontSize="10" Margin="0,3,0,0" TextWrapping="Wrap"/>
                    <CheckBox Name="Driver1ExternalCheckBox" Content="External (not bundled in pack)" Foreground="#E8A000" FontSize="12" Margin="0,5,0,3"
                              Checked="Driver1External_Changed" Unchecked="Driver1External_Changed"/>
                    <StackPanel Name="Driver1ExternalPanel" Visibility="Collapsed" Margin="0,0,0,3">
                        <TextBlock Text="Archive source path:" Foreground="#888888" FontSize="11" Margin="0,0,0,3"/>
                        <TextBox Name="Driver1SourcePathTextBox" Style="{StaticResource ModernTextBox}" TextChanged="Driver1SourcePath_TextChanged"/>
                    </StackPanel>
                </StackPanel>

                <!-- Driver 2 Livery -->
                <Label Content="Driver 2 Livery:"/>
                <StackPanel Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Name="Driver2LiveryTextBox" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Browse..." Style="{StaticResource SecondaryButton}" Click="BrowseDriver2Livery_Click"/>
                    </Grid>
                    <TextBlock Name="Driver2LiverySourceLabel" Text="" Foreground="#888888" FontSize="10" Margin="0,3,0,0" TextWrapping="Wrap"/>
                    <CheckBox Name="Driver2ExternalCheckBox" Content="External (not bundled in pack)" Foreground="#E8A000" FontSize="12" Margin="0,5,0,3"
                              Checked="Driver2External_Changed" Unchecked="Driver2External_Changed"/>
                    <StackPanel Name="Driver2ExternalPanel" Visibility="Collapsed" Margin="0,0,0,3">
                        <TextBlock Text="Archive source path:" Foreground="#888888" FontSize="11" Margin="0,0,0,3"/>
                        <TextBox Name="Driver2SourcePathTextBox" Style="{StaticResource ModernTextBox}" TextChanged="Driver2SourcePath_TextChanged"/>
                    </StackPanel>
                </StackPanel>

                <!-- Helmet Sponsors -->
                <Label Content="Helmet Sponsors:"/>
                <StackPanel Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Name="HelmetSponsorsTextBox" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Browse..." Style="{StaticResource SecondaryButton}" Click="BrowseHelmetSponsors_Click"/>
                    </Grid>
                    <TextBlock Name="HelmetSponsorsSourceLabel" Text="" Foreground="#888888" FontSize="10" Margin="0,3,0,0" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Visor Sponsors -->
                <Label Content="Visor Sponsors:"/>
                <StackPanel Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Name="VisorSponsorsTextBox" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Browse..." Style="{StaticResource SecondaryButton}" Click="BrowseVisorSponsors_Click"/>
                    </Grid>
                    <TextBlock Name="VisorSponsorsSourceLabel" Text="" Foreground="#888888" FontSize="10" Margin="0,3,0,0" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Livery Preview -->
                <Label Content="Livery Preview:"/>
                <StackPanel Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Name="LiveryPreviewTextBox" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Browse..." Style="{StaticResource SecondaryButton}" Click="BrowseLiveryPreview_Click"/>
                    </Grid>
                    <TextBlock Name="LiveryPreviewSourceLabel" Text="" Foreground="#888888" FontSize="10" Margin="0,3,0,0" TextWrapping="Wrap"/>
                    <CheckBox Name="PreviewExternalCheckBox" Content="External (not bundled in pack)" Foreground="#E8A000" FontSize="12" Margin="0,5,0,3"
                              Checked="PreviewExternal_Changed" Unchecked="PreviewExternal_Changed"/>
                    <StackPanel Name="PreviewExternalPanel" Visibility="Collapsed" Margin="0,0,0,3">
                        <TextBlock Text="Archive source path:" Foreground="#888888" FontSize="11" Margin="0,0,0,3"/>
                        <TextBox Name="PreviewSourcePathTextBox" Style="{StaticResource ModernTextBox}" TextChanged="PreviewSourcePath_TextChanged"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Click="OK_Click" Width="100" Margin="0,0,10,0" Background="#E10600" Foreground="White" BorderThickness="0" Padding="10" FontWeight="SemiBold" Cursor="Hand"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="100" Background="#404040" Foreground="White" BorderThickness="0" Padding="10" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
