<Window x:Class="AMS2ChEd.SeasonPackEditor.LiverySelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Livery" Height="400" Width="500" WindowStartupLocation="CenterOwner" Background="#1A1A1A">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Select Livery to Import" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#E10600" 
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   Name="MessageTextBlock"
                   Text="Select a livery configuration from the XML file:" 
                   Foreground="#FFFFFF" 
                   FontSize="13"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <ListBox Grid.Row="2" 
                 Name="LiveryListBox" 
                 Style="{StaticResource ModernListBox}"
                 Margin="0,0,0,20"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" 
                    Click="OK_Click" 
                    Width="100" 
                    Margin="0,0,10,0" 
                    Background="#E10600" 
                    Foreground="White" 
                    BorderThickness="0" 
                    Padding="10" 
                    FontWeight="SemiBold"/>
            <Button Content="Cancel" 
                    Click="Cancel_Click" 
                    Width="100" 
                    Background="#404040" 
                    Foreground="White" 
                    BorderThickness="0" 
                    Padding="10"/>
        </StackPanel>
    </Grid>
</Window>
